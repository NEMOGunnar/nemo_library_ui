{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Run UI",
        "type": "python",
        "request": "launch",
        "program": "${workspaceFolder}/ui.py",
        "cwd": "${workspaceFolder}",
        "console": "integratedTerminal"
      }
    ]
  }